{
  "name": "svg-path-length",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy-and-build": "npm run build:client && npm run deploy",
    "deploy": "ts-node --project deploy/tsconfig.json deploy",
    "build:client": "lerna run build --scope=svg-path-length-client"
  },
  "devDependencies": {
    "lerna": "^3.13.1",
    "node-ssh": "^6.0.0",
    "ts-node": "^8.4.1"
  }
}
